@page
@model Tanulok_proba.Pages.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Tanar</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Tanar.Nev)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Tanar.Nev)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Tanar?.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
